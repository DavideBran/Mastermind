export const environment = {
    production: true, 
    apiEndpoint: document.location.protocol + "//" + document.location.hostname + ":" + document.location.port
}

// ng serve --host 0.0.0.0 --disable-host-check